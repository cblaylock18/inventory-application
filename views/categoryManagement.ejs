<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><% title%></title>
        <link rel="stylesheet" href="/styles.css" />
    </head>
    <body>
        <h1><% title%></h1>
        <a href="/">Home</a>
        <%- include("partials/errors.ejs") %>
        <form action="/categoryManagement/<%= locals.category ? category.id : "new" %>" method="post">
            <label for="type">Animal's Type: </label>
            <input type="text" name="type" id="type" value="<%= locals.category ? category.type : "" %>" required />
            <label for="avglifespan">Average Lifespan: </label>
            <input
                type="number"
                name="avglifespan"
                id="avglifespan"
                min="0"
                step="any"
                value="<%= locals.category ? category.avglifespan : "" %>"
                required
            />
            <button type="submit"><%= locals.product ? "Update" : "Create" %></button>
        </form>
        <% if (category) { %>
        <form action="/categoryManagement/deleteCategory/<%= category ? category.id : "" %>" method="post">
            <button type="submit" onclick="return confirm('Are you sure you want to delete this animal type?')">Delete</button>
        </form>
        <% } %>
    </body>
</html>
