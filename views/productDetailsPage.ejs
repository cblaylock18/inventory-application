<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %></title>
        <link rel="stylesheet" href="/styles.css" />
    </head>
    <body>
        <h1><%= title %></h1>
        <div class="product-anchors">
            <a class="home-link" href="/">Home</a>
        </div>
        <% if (locals.product) { %>
        <section>
            <article class="product-details">
                <h3><%= product.petname %></h3>
                <p>
                    <span>Type:</span>
                    <a href="/category/view/<%= product.animalid %>"
                        ><%= product.type %></a
                    >
                </p>
                <p><span>Average Lifespan: </span><%= product.avglifespan %></p>
                <p>
                    <span>Foster Family:</span>
                    <a href="/user/view/<%= product.userid %>"
                        ><%= product.name %></a
                    >
                </p>
                <p><span>Phone: </span><%= product.phone %></p>
                <p><span>Street: </span><%= product.street %></p>
                <p><span>City: </span><%= product.city %></p>
                <p><span>State: </span><%= product.state %></p>
                <p><span>Zip: </span><%= product.zip %></p>
                <p><span>Price: </span><%= product.price %></p>
                <div class="view-edit-anchors">
                    <a class="edit" href="/productManagement/<%= product.id %>"
                        >Edit</a
                    >
                </div>
            </article>
        </section>
        <% } %>
    </body>
</html>
